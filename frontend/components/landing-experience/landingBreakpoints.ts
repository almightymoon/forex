/**
 * Landing “mobile” = phones only.
 * Tablets (768px+) use the desktop layout and scroll animations.
 */
export const LANDING_MOBILE_MAX_PX = 767;

export const LANDING_MOBILE_MQ = `(max-width: ${LANDING_MOBILE_MAX_PX}px)`;
